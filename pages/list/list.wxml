<!--index.wxml-->
<view class="box">
  <block wx:for="{{localList}}" wx:key="{{item}}">
    <view bindtap="detailRoad" data-detail="{{item.list}}">
      {{item.starttime}}——{{item.endtime}}
    </view>
  </block>
</view>
